# 农作物病害识别系统部署计划

## 目标

在当前 Windows 工作区完成项目的可运行部署，并验证前端、Spring Boot 后端、Flask 推理服务及数据库之间的基本连通性。

## 阶段

- [in_progress] 1. 读取部署要求并盘点运行环境
- [pending] 2. 准备数据库、Python、Java 与前端依赖
- [pending] 3. 启动三项服务并处理配置问题
- [pending] 4. 做 HTTP、页面和推理链路验证
- [pending] 5. 汇总启动方式、访问地址和遗留条件

## 约束

- 保留用户现有改动，不重置或覆盖业务代码。
- 不在源码中写入真实 API 密钥。
- 数据库初始化前先确认本机 MySQL 状态和 SQL 文件中的数据库名。

## 错误记录

| 阶段 | 错误 | 处理 |
|---|---|---|
| 1 | 外层目录不是 Git 根目录 | 已定位到内层项目目录，继续按文件部署 |
| 1 | PATH 中无 Java、Maven、MySQL、FFmpeg、Python | 已开始在 E 盘定位绝对路径；FFmpeg/Python 已找到，其余待确认 |
