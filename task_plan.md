# 番茄温室智能体一体化系统实施计划

## 目标

在不破坏既有病害识别、温室、库存、采购和 AI 问答接口的前提下，交付单一番茄温室的可重复仿真、受约束策略决策、虚拟执行与统一前端运行状态。

## 阶段

- [complete] 1. 确认竞赛匹配、系统边界、设计说明和实现计划
- [in_progress] 2. 完成隔离迁移、确定性推演引擎与事务性运行生命周期
- [in_progress] 3. 接入统一 API/Pinia 状态到指挥中心和既有页面
- [pending] 4. 补齐视觉证据、AI 解释保护边界与使用说明
- [pending] 5. 执行后端测试、前端生产构建、静态审查和 Git 提交

## 已确认约束

- 仅模拟 8 号番茄温室；所有新数据明确标记 `SIMULATED`、`MANUAL`、`LEGACY_HISTORY` 或 `VISION_SIGNAL`。
- 不重导入、不修改既有 `cropdisease.sql`，不扣减旧 `storage` 的设备本体库存。
- 15 分钟虚拟步长和固定种子必须产生相同快照、策略、动作与资源流水。
- 深度模型只能解释已保存的结构化策略，不能绕开规则直接设备控制。
- YOLO 标签和置信度尚未校准，导入视觉记录必须保持待核验，不能触发药剂或自动处置。

## 当前执行分工

- 后端：补齐 `agent` 运行服务、控制器、调度与事务性持久化。
- 前端：将首页、温室、识别、库存采购等既有页面接到共享 Pinia 运行状态。
- 主执行：审查接口形状、补齐解释/视觉接口、构建验证、修复并提交。

## 错误记录

| 阶段 | 错误 | 处理 |
|---|---|---|
| 1 | 外层工作目录不是 Git 根目录 | 已定位到内层项目根目录，后续 Git 操作使用该路径。 |
| 5 | 先前 Maven 静默测试长时间无输出被终止 | 后端完成后改用可见输出的 Maven 运行时执行测试。 |
